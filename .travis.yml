language: ruby
rvm:
  - 2.4.1
before_script:
  - psql -c 'create database bon_voyage_api_test;' -U postgres
script:
  - rake db:rollback
  - rake db:migrate
  - rake db:seed
  - bundle exec rspec
services:
  - postgresql
env:
  - NODE_ENV="test"
